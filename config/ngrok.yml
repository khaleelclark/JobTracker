version: "2"
# Optional helper for users who choose ngrok.
# Install your token separately with:
#   ngrok config add-authtoken <token>

tunnels:
  mcp:
    addr: 7331
    proto: http
    inspect: true
